import { AboutPageClient } from '@/components/AboutPageClient';

export const metadata = {
  title: 'About Us | Career For Everyone',
};

export default function AboutPage() {
  return <AboutPageClient />;
}
